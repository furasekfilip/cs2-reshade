First extract the injector.zip, go into the injector folder then copy its path.
Then run a admin cmd(mandatory due to dll injection), and cd into that copied path.
Then run this command "inject_x64.exe cs2.exe"(do it before cs2 starts)
Now run CS2 you may notice stutter at the start but the performance is ok after it.
Enjoy!
